#include <stdio.h>
#include <stdlib.h>
#include "funcion.h"



/**typedef struct {
    char apellido[31];
    char nombre[31];
    int edad ;
    int isEmpty;
}persona;*/

persona alta(persona){
  printf("ingrese apellido -nombre - edad ");
    scanf("%s",per.apellido);
    fflush(stdin);
    scanf("%s",per.nombre );
    fflush(stdin);
    scanf("%d",&per.edad);
    per.isEmpty=0;


 }
